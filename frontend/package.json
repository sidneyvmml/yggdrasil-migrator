{
  "name": "yggdrasil-migrator-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"API,UI\" --prefix-colors \"cyan,green\" --kill-others-on-fail \"cd .. && python -m uvicorn backend.app.main:app --host 127.0.0.1 --port 8000 --reload\" \"vite\"",
    "dev:ui": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "axios": "^1.5.0",
    "pinia": "^3.0.4",
    "vue": "^3.5.3"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.4",
    "@vue/eslint-config-prettier": "^10.2.0",
    "@vue/tsconfig": "^0.9.1",
    "concurrently": "^10.0.3",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
