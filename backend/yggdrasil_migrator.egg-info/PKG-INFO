Metadata-Version: 2.4
Name: yggdrasil-migrator
Version: 0.1.0
Summary: MongoDB migration orchestrator with FastAPI, Celery, and Vue 3.
Requires-Python: >=3.11
Description-Content-Type: text/markdown
Requires-Dist: fastapi>=0.110.0
Requires-Dist: uvicorn[standard]>=0.23.0
Requires-Dist: motor<4.0,>=3.6.0
Requires-Dist: celery[redis]>=5.3.0
Requires-Dist: pydantic>=2.6.0
Requires-Dist: pydantic-settings>=2.0.0
Requires-Dist: python-dotenv>=1.0.0
Provides-Extra: dev
Requires-Dist: pytest>=8.0; extra == "dev"
Requires-Dist: pytest-asyncio>=0.22.0; extra == "dev"
Requires-Dist: httpx>=0.26.0; extra == "dev"

# Yggdrasil Migrator

Backend: FastAPI + Celery + Redis + Motor
Frontend: Vue 3 + Pinia + Tailwind

## Run backend

1. Install dependencies:

```bash
python -m pip install -e .
pip install -r requirements-dev.txt
```

2. Start Redis

3. Start Celery worker:

```bash
cd backend
celery -A app.workers.celery_app.celery worker --loglevel=info
```

4. Start FastAPI:

```bash
cd backend
uvicorn app.main:app --reload
```

## Run frontend

```bash
cd frontend
npm install
npm run dev
```
